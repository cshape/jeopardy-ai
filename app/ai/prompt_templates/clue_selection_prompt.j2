Please evaluate if the following message from a player is a valid request to select a clue on the Jeopardy board.

Player message: "{{ player_message }}"

Available categories and clues on the board:
{% for category in available_categories %}
Category: {{ category.name }}
Available values: {{ category.available_values|join(", ") }}
{% endfor %}

Determine if this is a valid clue selection and which category/value the player wants. 